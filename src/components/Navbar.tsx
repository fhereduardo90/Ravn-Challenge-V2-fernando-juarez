const Navbar = () => (
  <nav className="flex items-center justify-between flex-wrap bg-ravn-black px-8 py-8 w-screen">
    <span className="font-semibold text-2xl leading-8 text-ravn-white tracking-tight">
      Ravn Star Wars Registry
    </span>
  </nav>
);

export default Navbar;
